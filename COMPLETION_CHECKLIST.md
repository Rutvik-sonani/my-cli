# MyCLI Completion Checklist

Track spec compliance by phase. Update status as work lands.

**Legend:** ✅ Done · ⚠️ Partial · ❌ Missing

**Overall (last audit):** ~88% → **~92%** after completion hardening (auth/RBAC ORM parity, env files, Enterprise SaaS, CodeQL, SQL Server+Drizzle, DEPLOYMENT.md on create, root ESLint/Prettier)

---

## Sprint 1 — Foundation ✅

| ID | Task | Status |
|----|------|--------|
| 0.1 | Tests for `cli-engine`, `config-manager`, `dependency-manager`, `prompt-engine`, `telemetry-manager` | ✅ |
| 0.3 | CLI integration tests (`apps/cli/tests/commands.test.ts`) | ✅ |
| 0.5 | Husky `pre-push` + `commit-msg` | ✅ |
| 1.1 | i18n module in `@mycli/prompt-engine` | ✅ |
| 1.2 | Locales `en`, `hi`, `es`, `fr` | ✅ |
| 1.3 | Create wizard uses `engine.i18n.t()` | ✅ |

**Notes:**
- Set locale: `MYCLI_LOCALE=hi my create`
- Locales copied to `apps/cli/dist/locales` on build
- Commit messages must follow Conventional Commits (husky `commit-msg`)

---

## Sprint 2 — Generators + DB ✅

| ID | Task | Status |
|----|------|--------|
| 2.1 | Real Prisma migrations in `my make migration` | ✅ |
| 2.2 | Drizzle migration generation | ✅ |
| 2.3 | TypeORM/Sequelize migration generation | ✅ |
| 2.6 | CRUD emits migrations | ✅ |
| 3.10 | Generate `ENVIRONMENT.md` on create | ✅ |

---

## Sprint 3 — Create wizard + Git ✅

| ID | Task | Status |
|----|------|--------|
| 5.1 | Git init: LICENSE, CHANGELOG, CONTRIBUTING, etc. | ✅ |
| 5.2 | Interactive git provider in `my create` | ✅ |
| 5.3 | Branch strategy prompt | ✅ |
| 5.4 | Commit convention prompt | ✅ |
| 5.5 | Git hooks prompt in create | ✅ |
| 5.6 | CI/CD prompt in create | ✅ |
| 5.9 | GitHub `deploy.yml` workflow | ✅ |
| 5.11 | Renovate config generator | ✅ |

**Notes:**
- Non-interactive flags: `--branch-strategy`, `--commit-convention`, `--git-hooks`, `--cicd`, `--publish`
- GitHub CI uses `@mycli/github-manager` (includes deploy + Renovate); other providers use `@mycli/cicd-manager`

---

## Sprint 4 — Services breadth ✅

| ID | Task | Status |
|----|------|--------|
| 8.1 | Cache: Memory, Memcached | ✅ |
| 8.2 | Queue: RabbitMQ, Kafka, SQS | ✅ |
| 2.4 | `my make queue <name>` | ✅ |
| 2.5 | `my make mail <name>` | ✅ |
| 9.1 | `my backup` command | ✅ |

**Notes:**
- Cache providers: `my add cache --provider memory|memcached|redis`
- Queue providers: `my add queue --provider bullmq|rabbitmq|kafka|sqs`
- `my make queue <name>` → `src/jobs/<name>/`
- `my make mail <name>` → `src/mail/<name>.mail.ts`
- `my backup run|list` uses `@mycli/backup-manager`

---

## Sprint 5 — Cloud + Infra ✅

| ID | Task | Status |
|----|------|--------|
| 4.1 | `docker-compose.test.yml` | ✅ |
| 4.5 | AWS terraform module split | ✅ |
| 6.1 | `@mycli/azure` plugin | ✅ |
| 6.2 | `@mycli/gcp` plugin | ✅ |
| 4.7 | Unified `DEPLOYMENT.md` | ✅ |

**Notes:**
- Test compose: `docker compose -f docker-compose.test.yml up --abort-on-container-exit`
- AWS Terraform split: `network.tf`, `database.tf`, `storage.tf` under `deploy/terraform/aws/`
- `DEPLOYMENT.md` generated by `my deploy setup` and `my deploy terraform`

---

## Sprint 6 — Marketplace + docs ✅

| ID | Task | Status |
|----|------|--------|
| 0.2 | Official plugin tests (21 plugins) | ✅ |
| 1.6 | Populate root `templates/` or document layout | ✅ |
| 1.7 | Populate `docs/` | ✅ |
| 1.8 | `examples/` starter projects | ✅ |
| 7.1 | Community plugin example | ✅ |

**Notes:**
- Plugin smoke tests: `plugins/official-tests/`
- Community example: `plugins/community/hello-mycli/`
- Template source of truth: `apps/cli/templates/` (see `templates/README.md`)

---

## Phase summary

| Phase | Before | After Sprint 1 |
|-------|--------|----------------|
| 1 Core | 92% | **98%** |
| Testing bar | 48% | **58%** |
| i18n | 10% | **75%** |
| **Overall** | **~68%** | **~72%** → **~76%** |

See prior audit in chat for full 96-task breakdown.

---

## Sprint 7 — Auth & RBAC depth ✅ (partial)

| ID | Task | Status |
|----|------|--------|
| 7.1 | OAuth (arctic) authorize + callback | ✅ |
| 7.2 | Session (@fastify/secure-session plugin) | ✅ |
| 7.3 | Magic link + OTP verify + storage | ✅ |
| 7.4 | MFA (otplib TOTP) + Passkeys (WebAuthn) | ✅ |
| 7.5 | Drizzle user repository | ✅ |
| 7.6 | RBAC CLI ↔ DB sync (`my rbac sync`) | ✅ |
| 7.7 | Resource-aware `can()` + Prisma auth models patch | ✅ |

**Notes:**
- `my add auth` patches `prisma/schema.prisma` with OAuth/MagicLink/OTP/Passkey models when using Prisma
- `my role` / `my permission` auto-sync to DB when `.mycli/sync-rbac.ts` exists (requires `tsx` in project)
- Email/SMS delivery for magic link/OTP logs in dev; wire `my add mail` for production

---

## Sprint 8 — Database & ORM Parity ✅

| ID | Task | Status |
|----|------|--------|
| 8.1 | MikroORM migrations (no Prisma fallback) | ✅ |
| 8.2 | Drizzle dialect per database (pg/mysql/sqlite) | ✅ |
| 8.3 | ORM seeders (Drizzle, TypeORM, Mongoose, Sequelize, MikroORM) | ✅ |
| 8.4 | DatabasePlugin interface + full postgres plugin | ✅ |

**Notes:**
- `my make migration --orm mikroorm` writes MikroORM migration files
- MySQL projects get `drizzle-orm/mysql-core` schema/client templates
- SQL Server + Drizzle rejected at compatibility layer
- `registerDatabasePlugin()` generates `docs/migrations-<engine>.md` and `docker/database-<engine>.yml` on setup
- Seed scripts at `src/database/seed.ts` for each non-Prisma ORM

---

## Sprint 9 — Generated Project Quality ✅

| ID | Task | Status |
|----|------|--------|
| 9.1 | JavaScript project generation (`--language javascript`) | ✅ |
| 9.2 | Biome + optional ESLint/Prettier (`my add quality`) | ✅ |
| 9.3 | Commitizen + `pnpm commit` via git hooks | ✅ |
| 9.4 | Calendar versioning option (`--version-strategy calver`) | ✅ |

**Notes:**
- `my create js-app --yes --language javascript` → `src/index.js`, no `tsconfig.json`, JS vitest configs
- Every project gets `biome.json`; use `--eslint --prettier` or `my add quality` for alternate toolchain
- Git hooks add `.cz-config.js`, `commitizen`, and `pnpm commit`
- Git init during create configures release files; calver uses `release.calver.config.js`

---

## Sprint 10 — Git, CI/CD & Release Automation ✅

| ID | Task | Status |
|----|------|--------|
| 10.1 | Bitbucket remote automation (REST API + `bb` CLI) | ✅ |
| 10.2 | Azure DevOps end-to-end (`--org`, `--project`, remote + push) | ✅ |
| 10.3 | GitHub labels via `gh label create` (`--labels`) | ✅ |
| 10.4 | `my add github --deploy --renovate` parity with create | ✅ |

**Notes:**
- Bitbucket: set `BITBUCKET_TOKEN` and `--owner <workspace>` (or `BITBUCKET_WORKSPACE`)
- Azure: `my git publish --provider azure-devops --org contoso --project platform`
- Labels: `my git publish --provider github --labels` after repo creation
- `my add github --release --deploy` generates release + deploy + renovate workflows

---

## Sprint 11 — Runtime Features ✅

| ID | Task | Status |
|----|------|--------|
| 11.1 | Telemetry transport (`MYCLI_TELEMETRY_URL`, flush on shutdown) | ✅ |
| 11.2 | `my upgrade` migration engine (`@mycli/upgrade-manager`) | ✅ |
| 11.3 | `my doctor` DB ping + dependency audit | ✅ |
| 11.4 | `my security audit` + secret scan | ✅ |

**Notes:**
- Telemetry: opt-in only; batched POST on CLI shutdown when `MYCLI_TELEMETRY_URL` is set
- Upgrade: `.mycli/upgrade-state.json` tracks applied migrations; `--force` overwrites scaffolds
- Doctor: TCP database reachability from `DATABASE_URL`; `--skip-audit` skips npm/pnpm audit
- Security: `my security audit`, `my security scan-secrets`; SECURITY.md documents Snyk/Trivy CI

---

## Sprint 12 — Platform & DX ✅

| ID | Task | Status |
|----|------|--------|
| 12.1 | Multi-tenancy modes (single-db / schema-per-tenant / db-per-tenant) | ✅ |
| 12.2 | CLI i18n for doctor, upgrade, security, git + hi/es/fr locales | ✅ |
| 12.3 | Node toolchain in create (nvm / Volta / asdf) | ✅ |
| 12.4 | UI library “Other” in create wizard | ✅ |

**Notes:**
- Tenancy: `my add tenancy --mode <strategy>`; mode-specific middleware, resolver, and `docs/tenancy.md`
- i18n: set `MYCLI_LOCALE=hi` (or `es`, `fr`) for localized command intros/outros
- Toolchain: `--node-toolchain volta|asdf|nvm|none` during `my create`
- UI Other: `--ui-package <npm-name>` installs a custom npm UI library on the frontend

---

## Sprint 13 — Documentation & Repo Hygiene ✅

| ID | Task | Status |
|----|------|--------|
| 13.1 | Package READMEs for all managers missing docs | ✅ |
| 13.2 | Root OSS files (CODE_OF_CONDUCT, SECURITY, `.github/` templates) | ✅ |
| 13.3 | Spec/doc alignment (`apps/cli` vs `cli-engine`, `plugins.json`, guides) | ✅ |

**Notes:**
- Every `packages/*` package now has a README with CLI commands, outputs, and test command
- Root `.github/ISSUE_TEMPLATE/`, `dependabot.yml`, and PR template added for the MyCLI repo
- `ARCHITECTURE.md` documents `apps/cli` (binary + templates) vs `@mycli/cli-engine` (library orchestrator)
- `PLUGIN_GUIDE.md` / `MARKETPLACE_GUIDE.md` document `plugins/plugins.json` location and layout

---

## Sprint 14 — Test & E2E Hardening ✅

| ID | Task | Status |
|----|------|--------|
| 14.1 | Command-level integration tests (add/make/git/deploy/security/rbac/workflow) | ✅ |
| 14.2 | Generated app E2E smoke script + nightly CI workflow | ✅ |
| 14.3 | Auth/RBAC generated output TypeScript parse tests | ✅ |

**Notes:**
- Dry-run coverage: `my add auth|rbac|database --dry-run`, `my make module --dry-run`
- New CLI test files: `git.test.ts`, `deploy.test.ts`, `security.test.ts`, `rbac.test.ts`, `workflow.test.ts`
- E2E: `pnpm e2e:generated` or `.github/workflows/e2e-generated.yml` (nightly + manual); smoke runs create → install → build → unit tests
- Manager tests use `assertTypeScriptParses()` on generated `.ts` files

---

## Sprint 16 — Spec gap closure ✅

| ID | Task | Status |
|----|------|--------|
| 16.1 | Auth user repository for TypeORM/Mongoose/Sequelize/MikroORM | ✅ |
| 16.2 | RBAC repository for Prisma/Drizzle (memory fallback for others) | ✅ |
| 16.3 | `.env.local`, `.env.development`, `.env.production`, `.env.test` | ✅ |
| 16.4 | Enterprise SaaS modules (orgs, teams, billing) | ✅ |
| 16.5 | SQL Server + Drizzle (`schema.mssql.ts`, `client.mssql.ts`) | ✅ |
| 16.6 | `DEPLOYMENT.md` on every `my create` | ✅ |
| 16.7 | GitHub CodeQL workflow (`.github/workflows/codeql.yml`) | ✅ |
| 16.8 | Root ESLint + Prettier (`eslint.config.js`, `.prettierrc.json`) | ✅ |
| 16.9 | Git init fallback + local identity for CI/sandbox | ✅ |
| 16.10 | CLI integration test fix (git community files) | ✅ |

**Notes:**
- Auth ORM templates: `user.repository.{typeorm,mongoose,sequelize,mikroorm}.ts.ejs`
- RBAC: `rbac.repository.{prisma,drizzle}.ts.ejs` + memory fallback
- Enterprise: `my create shop --app-type enterprise-saas` enables auth/rbac/swagger + SaaS modules
- Run `pnpm lint:eslint` and `pnpm format:prettier` alongside Biome

---

| ID | Task | Status |
|----|------|--------|
| 15.1 | Fix Prisma schema: gate auth/RBAC relations on `includeAuth` / `includeRbac` | ✅ |
| 15.2 | Merge feature deps into `package.json` during create (even with `--skip-install`) | ✅ |
| 15.3 | Magic link / OTP mail delivery via `my add mail` in production | ✅ |
| 15.4 | Full E2E: default create + Prisma generate + full test suite | ✅ |
| 15.5 | npm publish workflow docs (`PUBLISHING.md`) | ✅ |
| 15.6 | Full CLI i18n on remaining commands (add/make/deploy/analytics/backup/plugin/ai/rbac/workflow) | ✅ |
| 15.7 | Live-server integration test in E2E (`TEST_BASE_URL` + `/health`) | ✅ |

**Notes:**
- `my create --skip-install` now writes `@prisma/client`, `supertest`, auth deps, etc. into `package.json`
- Prisma projects get `db:generate`, `db:push`, and `db:seed` scripts when ORM is Prisma
- Magic link / OTP templates send mail when `MAIL_PROVIDER` is set; dev mode still logs links/codes
- E2E: `pnpm e2e:generated` runs default PostgreSQL + Prisma path with live `/health` integration; set `MYCLI_E2E_MINIMAL=1` for no-DB smoke or `MYCLI_E2E_LIVE=0` to skip server
- i18n: all major commands localized; set `MYCLI_LOCALE=hi|es|fr` for Hindi, Spanish, or French
- Publishing: `pnpm release` via Changesets — see [PUBLISHING.md](./PUBLISHING.md)

---

## Sprint 17 — RBAC parity, AWS modules, docs site (2026-07-22)

| ID | Task | Status |
|----|------|--------|
| 17.1 | RBAC entities for TypeORM, Mongoose, Sequelize, MikroORM | ✅ |
| 17.2 | RBAC repositories for all persisted ORMs | ✅ |
| 17.3 | Multi-ORM `sync-rbac.ts` (Prisma, Drizzle, TypeORM, Mongoose, Sequelize, MikroORM) | ✅ |
| 17.4 | AWS Terraform: EKS, Lambda + API Gateway, CloudFront modules | ✅ |
| 17.5 | Docs website from repository markdown (`apps/website`) | ✅ |
| 17.6 | GitHub Pages workflow (`.github/workflows/docs.yml`) | ✅ |
| 17.7 | Publishing docs: CI release + official plugins (`PUBLISHING.md`) | ✅ |

**Notes:**
- RBAC DB entities emitted when `includeRbac: true` during database setup
- AWS terraform now includes `eks.tf`, `lambda.tf`, `cloudfront.tf` alongside ECS Fargate
- Docs site: `pnpm --filter @mycli/website build` → `apps/website/dist`
- Overall compliance estimate: **~95% structural / ~90% production-depth**

---

Detailed file-level tasks: [IMPLEMENTATION_BACKLOG.md](./IMPLEMENTATION_BACKLOG.md)
