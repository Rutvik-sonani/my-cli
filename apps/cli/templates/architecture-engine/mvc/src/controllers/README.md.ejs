# Controllers

HTTP adapters for <%= appName %>. One controller per resource or feature area.

- Parse and validate input (delegate to validators/DTOs)
- Call services — no business logic here
- Map service results to HTTP responses

Example: `user.controller.ts` → `UserService`
