# Services

Application business logic for <%= appName %>.

- Implement use cases invoked by controllers
- Depend on repository interfaces, not concrete persistence
- Throw domain errors; let controllers map to HTTP status codes
