<% if (language === 'javascript') { %>
/**
 * Template marketplace types for <%= appName %>
 * @typedef {'public'|'private'|'organization'} TemplateVisibility
 * @typedef {Object} TemplateRequirements
 * @property {string[]} [features]
 * @property {string[]} [database]
 * @property {string[]} [architectureStyles]
 * @property {string} [minCliVersion]
 * @typedef {Object} MarketplaceTemplate
 * @property {string} id
 * @property {string} name
 * @property {string} version
 * @property {string} author
 * @property {string} description
 * @property {TemplateVisibility} visibility
 * @property {string} [organization]
 * @property {string} compatibility
 * @property {TemplateRequirements} [requirements]
 * @property {string[]} [tags]
 * @property {number} [downloads]
 */

export {};
<% } else { %>
/**
 * Template marketplace types for <%= appName %>
 */
export type TemplateVisibility = 'public' | 'private' | 'organization';

export interface TemplateRequirements {
  features?: string[];
  database?: string[];
  architectureStyles?: string[];
  minCliVersion?: string;
}

export interface MarketplaceTemplate {
  id: string;
  name: string;
  version: string;
  author: string;
  description: string;
  visibility: TemplateVisibility;
  organization?: string;
  compatibility: string;
  requirements?: TemplateRequirements;
  tags?: string[];
  downloads?: number;
}

export interface TemplateSearchResult {
  templates: MarketplaceTemplate[];
  total: number;
}
<% } %>
