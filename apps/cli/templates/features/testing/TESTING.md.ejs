# Testing

Generated by MyCLI.

## Unit tests (<%= unit %>)

```bash
<% if (unit === 'vitest') { %>pnpm vitest run<% } else { %>pnpm jest<% } %>
```

<% if (integration) { %>
## Integration tests (Supertest)

```bash
TEST_BASE_URL=http://localhost:3000 <% if (unit === 'vitest') { %>pnpm vitest run tests/integration<% } else { %>pnpm jest tests/integration<% } %>
```
<% } %>

<% if (e2e === 'playwright') { %>
## E2E (Playwright)

```bash
pnpm playwright test
```
<% } else if (e2e === 'cypress') { %>
## E2E (Cypress)

```bash
pnpm cypress run
```
<% } %>
