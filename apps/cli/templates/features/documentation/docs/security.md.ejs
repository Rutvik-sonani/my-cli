# Security — <%= projectName %>

> Generated by MyCLI documentation engine. Customize for your team.

## Principles

- Least privilege by default
- Secrets never committed
- Defense in depth (authn, authz, validation, headers)
- Audit sensitive mutations

## Baseline controls

| Control | Status guidance |
|---------|-----------------|
| Authentication | Enable via `my add auth` / `my add enterprise-auth` |
| RBAC | Enable via `my add rbac` |
| Security platform | Enable via `my security setup` |
| Dependency audit | Run `my security audit` |
| Secret scan | Run `my security scan` / `my security scan-secrets` |

<% if (features && features.security) { %>
Security platform feature is enabled in this project.
<% } else { %>
Security platform is **not** enabled yet — run `my security setup`.
<% } %>

## Secrets

- Store secrets in environment variables or a secrets manager
- Keep `.env` out of git (use `.env.example`)
- Rotate credentials on incident or staff change

## Reporting vulnerabilities

1. Do not open a public issue with exploit details
2. Contact the security owner privately
3. Include reproduction steps and impact

## Related commands

```bash
my security setup
my security audit
my security scan
```
