# API Guide — <%= projectName %>

> Generated by MyCLI documentation engine. Customize for your team.

> **Related:** For OpenAPI / Swagger routes and client collections, see `API.md` (from `my add swagger` / api-docs). This file (`API_GUIDE.md`) covers conventions and examples.

## Conventions

- JSON request/response bodies
- Consistent error envelope
- Version APIs when breaking changes are required
- Authenticate mutating endpoints

## Base URL

```
http://localhost:3000
```

## Authentication

<% if (features && (features.auth || features['enterprise-auth'])) { %>
Auth is enabled. Prefer bearer tokens / session cookies as configured by your auth module.
<% } else { %>
Auth is not enabled yet. Add with `my add auth` before exposing protected routes.
<% } %>

## Example

```http
GET /health HTTP/1.1
Host: localhost:3000
```

```http
POST /api/v1/resources HTTP/1.1
Host: localhost:3000
Authorization: Bearer <token>
Content-Type: application/json

{ "name": "example" }
```

## Errors

Return stable machine-readable codes:

```json
{
  "error": {
    "code": "VALIDATION_FAILED",
    "message": "name is required"
  }
}
```

<% if (features && features['api-docs']) { %>
## OpenAPI

API docs feature is enabled — keep Swagger/OpenAPI artifacts updated.
<% } else { %>
## OpenAPI

Consider `my add api-docs` for interactive API documentation.
<% } %>

## Related commands

```bash
my add api-docs
my make controller <name>
my make crud <name>
```
