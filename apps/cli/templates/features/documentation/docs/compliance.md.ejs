# Compliance — <%= projectName %>

> Generated by MyCLI documentation engine. Customize for your team.

## Purpose

Document how **<%= projectName %>** meets regulatory and internal compliance expectations (GDPR, HIPAA, SOC2, ISO27001 as applicable).

## Control themes

- Data retention and deletion
- Access control and audit trails
- Privacy (consent, export, erase)
- Change management and evidence

<% if (features && features.compliance) { %>
Compliance engine feature is enabled.
<% } else { %>
Compliance scaffolding is **not** enabled — consider `my add compliance`.
<% } %>
<% if (features && features.privacy) { %>
Privacy tooling is enabled (`my privacy export` / `my privacy delete`).
<% } %>
<% if (features && features.audit) { %>
Audit logging is enabled for sensitive actions.
<% } %>

## Evidence checklist

- [ ] Retention policy documented
- [ ] Access reviews scheduled
- [ ] Incident response runbook reviewed
- [ ] Backup restore tested
- [ ] Vendor/DPA inventory current

## Related commands

```bash
my add compliance
my add privacy
my add audit
my governance check
```
