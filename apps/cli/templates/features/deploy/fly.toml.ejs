app = "<%= appName %>"
primary_region = "<%= region %>"

[build]

[http_service]
  internal_port = <%= port %>
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = <%= minReplicas %>
