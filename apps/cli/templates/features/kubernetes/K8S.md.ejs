# Kubernetes — <%= appName %>

Generated by MyCLI.

## Apply manifests

```bash
kubectl apply -f k8s/
```

## Verify

```bash
kubectl get all -n <%= appName %>
kubectl logs -n <%= appName %> -l app=<%= appName %>
```

## Files

- `namespace.yaml` — isolated namespace
- `deployment.yaml` — app deployment (<%= replicas %> replicas)
- `service.yaml` — ClusterIP service
- `ingress.yaml` — ingress at <%= host %>
- `configmap.yaml` — environment config
- `hpa.yaml` — horizontal pod autoscaler
