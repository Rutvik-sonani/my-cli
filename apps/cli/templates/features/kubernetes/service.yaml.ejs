apiVersion: v1
kind: Service
metadata:
  name: <%= appName %>
  namespace: <%= appName %>
  labels:
    app: <%= appName %>
spec:
  type: ClusterIP
  selector:
    app: <%= appName %>
  ports:
    - port: <%= port %>
      targetPort: <%= port %>
      protocol: TCP
      name: http
