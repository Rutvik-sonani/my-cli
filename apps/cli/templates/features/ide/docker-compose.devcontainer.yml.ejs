services:
  app:
    build:
      context: ..
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
    ports:
      - "<%= port %>:<%= port %>"
    environment:
      NODE_ENV: development
