# IDE & DevContainer Setup — <%= appName %>

Generated by MyCLI for local development in VS Code, Cursor, or GitHub Codespaces.

## VS Code / Cursor

| File | Purpose |
|------|---------|
| `.vscode/settings.json` | Editor defaults (format on save, TypeScript SDK) |
| `.vscode/extensions.json` | Recommended extensions |
| `.vscode/launch.json` | Debug configurations for dev server and tests |
| `.cursor/rules/mycli-project.mdc` | Cursor AI project rules |

Open the project folder in VS Code or Cursor and accept recommended extensions when prompted.

## DevContainer

| File | Purpose |
|------|---------|
| `.devcontainer/devcontainer.json` | Codespaces / Dev Containers configuration |
<% if (useDockerCompose) { -%>
| `.devcontainer/docker-compose.yml` | Compose service for the dev container |
<% } -%>

### Start DevContainer

1. Install the [Dev Containers](https://marketplace.visualstudio.com/items?itemName=ms-vscode-remote.remote-containers) extension.
2. Run **Dev Containers: Reopen in Container** from the command palette.
3. After build, run:

```bash
<%= devCommand %>
```

## Workflow commands

```bash
my dev      # <%= devCommand %>
my test     # <%= testCommand %>
my lint     # <%= lintCommand %>
my build    # build production bundle
```

## Ports

- App: `<%= port %>`

Package manager: **<%= packageManager %>**
