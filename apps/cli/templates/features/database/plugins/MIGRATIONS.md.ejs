# Database migrations — <%= title %>

Generated by MyCLI for **<%= orm %>** on **<%= database %>**.

## Commands

<% if (orm === 'prisma') { %>
- `pnpm exec prisma migrate dev`
- `pnpm exec prisma db seed`
<% } else if (orm === 'drizzle') { %>
- `pnpm exec drizzle-kit generate`
- `pnpm exec drizzle-kit migrate`
- `pnpm exec tsx src/database/seed.ts`
<% } else if (orm === 'typeorm') { %>
- `pnpm exec typeorm migration:run -d src/database/data-source.ts`
- `pnpm exec tsx src/database/seed.ts`
<% } else if (orm === 'sequelize') { %>
- Run migrations from `src/database/migrations/`
- `pnpm exec tsx src/database/seed.ts`
<% } else if (orm === 'mongoose') { %>
- `pnpm exec tsx src/database/seed.ts`
<% } else if (orm === 'mikroorm') { %>
- `pnpm exec mikro-orm migration:up`
- `pnpm exec tsx src/database/seed.ts`
<% } %>

Use `my make migration <name>` to generate entity migrations in this project.
