# <%= appName %> — Release Automation

Generated by MyCLI.

Strategy: **<%= strategy %>**

## Changesets

```bash
pnpm changeset
pnpm changeset version
pnpm changeset publish
```

## Semantic Release

Configure `NPM_TOKEN` and `GITHUB_TOKEN` secrets, then push to `<%= branch %>`.

<% if (strategy === 'calver') { %>
Calendar versioning uses `release.calver.config.js` (format `YYYY.MM.DD`).

```bash
pnpm exec semantic-release --extends ./release.calver.config.js
```
<% } else { %>
```bash
pnpm exec semantic-release --extends ./release.config.js
```
<% } %>
