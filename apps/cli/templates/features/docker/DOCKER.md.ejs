# Docker — <%= appName %>

Generated by MyCLI.

## Development

```bash
docker compose up --build
```

## Production

```bash
docker compose -f docker-compose.prod.yml up --build -d
```

## Testing / CI

```bash
docker compose -f docker-compose.test.yml up --build --abort-on-container-exit
```

## Services

- **app** — Node.js <%= nodeVersion %> on port <%= port %>
<% if (database === 'postgres') { %>- **postgres** — PostgreSQL 16
<% } %><% if (redis) { %>- **redis** — Redis 7
<% } %><% if (mailhog) { %>- **mailhog** — SMTP capture (8025 UI)
<% } %><% if (nginx) { %>- **nginx** — reverse proxy on port 80
<% } %>
