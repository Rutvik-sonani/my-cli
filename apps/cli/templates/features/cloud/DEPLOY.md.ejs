# <%= appName %> — Cloud Deployment (<%= provider %>)

Generated by MyCLI Phase 6.

Environment: **<%= environment %>**

## Commands

```bash
my deploy push --provider <%= provider %>
my deploy status --provider <%= provider %>
my deploy logs --provider <%= provider %>
my deploy secrets sync --provider <%= provider %> --dry-run
my deploy rollback --provider <%= provider %> --dry-run
my deploy destroy --provider <%= provider %> --dry-run
```

## Prerequisites

See `deploy/secrets.<%= provider %>.md` for required tokens and CLI tools.
