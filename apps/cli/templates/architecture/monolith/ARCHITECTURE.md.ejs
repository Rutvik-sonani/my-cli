# <%= appName %> — Monolith Architecture

Generated by MyCLI.

## Layout

```
src/
  config/     Shared configuration
  shared/     Cross-cutting utilities
  modules/    Feature modules (optional)
  index.ts    Application entry
```

## Guidelines

- Keep business logic in services, HTTP in controllers.
- Use `my make module <name>` to add feature modules.
