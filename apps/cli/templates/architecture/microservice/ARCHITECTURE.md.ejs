# <%= appName %> — Microservice Architecture

This project is structured for independent services under `services/`.

## Next steps

1. `my make module <service>` for each bounded context
2. Add Docker Compose per service
3. Configure API gateway / service mesh as needed
