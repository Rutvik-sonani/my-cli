# <%= appName %> — Monorepo

pnpm workspace with `apps/` for deployable applications and `packages/` for shared libraries.

## Layout

```
apps/          Deployable apps (api, web)
packages/      Shared libraries
pnpm-workspace.yaml
```
